# css-override-web-extension

This is a web extension intended to allow you to override the css on specific domains.

## TODO:

- Inject custom css onto a domain
- Blacklist specific rules on a site
- In-extension editor
- Allow for scss or less
